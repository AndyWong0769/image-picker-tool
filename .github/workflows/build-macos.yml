name: Build macOS DMG

on:
  push:
    tags:
      - 'v*'  # 推送 v* 标签时触发
  workflow_dispatch:  # 也支持手动触发

# 给 GITHUB_TOKEN 写权限，允许 Create Release
permissions:
  contents: write

jobs:
  build:
    # Apple Silicon runner (macos-13 Intel 已于2025年12月退役)
    runs-on: macos-15

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python (x86_64 for Rosetta cross-compile)
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          architecture: 'x64'  # x86_64 Python，兼容所有 Mac + 虚拟机

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install py2app pillow

      - name: Build macOS app (x86_64, min macOS 11 Big Sur)
        env:
          MACOSX_DEPLOYMENT_TARGET: '11.0'
        run: |
          python setup.py py2app

      - name: Create DMG
        run: |
          cd dist
          hdiutil create -volname "图片编号挑选工具" -srcfolder "图片编号挑选工具.app" -ov -format UDZO ../图片编号挑选工具.dmg
          cd ..

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: 图片编号挑选工具-macos
          path: 图片编号挑选工具.dmg
          retention-days: 30

      - name: Create Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v1
        with:
          files: 图片编号挑选工具.dmg
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
